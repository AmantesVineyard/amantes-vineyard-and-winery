# PostgreSQL connection string. The database needs the pg_trgm, unaccent and
# vector (pgvector) extensions available; migrations create them if the role
# has permission, otherwise create them as a superuser first.
DATABASE_URL=postgresql://winekb:winekb@localhost:5432/wine_directory
